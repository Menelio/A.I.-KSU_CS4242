package RoutFinder;

import javafx.scene.control.Label;
import javafx.scene.shape.Circle;

public class Node {
	//global Var
	Circle circle;
	//distance from goal
	int hn;
	Label name;
	/**<h1>Constructor</h1>
	 * Creats node based on given param
	 * @param name				: String name of this distination
	 * @param hn 				: int value of h(n)
	 */
	public nodeDistination()
}
